%= Preamble and macros for the print version, appended after the common preamble. =%

%== Margins and page layout. ==%
\usepackage[margin=2.54cm]{geometry}

%== Add the bibliography to the table of contents. ==%
\usepackage[nottoc, numbib]{tocbibind}

%== Hyperlinks setup. ==%
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=magenta,
  citecolor=blue,
}

%== Compatibility for web-only Blueprint macros. ==%
\usepackage{expl3}

\newcommand{\lean}[1]{}
\newcommand{\discussion}[1]{}
\newcommand{\leanok}{}
\newcommand{\mathlibok}{}
\newcommand{\notready}{}

\ExplSyntaxOn
\NewDocumentCommand{\uses}{m}%
  {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}\ignorespaces}
\NewDocumentCommand{\proves}{m}%
  {\clist_map_inline:nn{#1}{\vphantom{\ref{##1}}}\ignorespaces}
\ExplSyntaxOff
